services:
  app:
    build:
      context: .
      dockerfile: infra/Dockerfile
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      # Mimic Cloud Run so the WebSocket voice component is shown
      - K_SERVICE=local-dev
